package com.kwang.study.mathvision.workflow.model;

/** Shared execution mode for stages that support user-requested regeneration. */
public enum StageGenerationMode {
    INITIAL_GENERATION,
    USER_REVISION
}

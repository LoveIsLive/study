<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kwang.study.course.mapper.CourseMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course (class_id, teacher_id, `name`, description, cover_image)
        VALUES (#{classId}, #{teacherId}, #{name}, #{description}, #{coverImage})
    </insert>

    <update id="update">
        UPDATE course
        <set>
            <if test="name != null">`name` = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <!-- 注意：仅删除课程表记录，不做任何级联清理 -->
    <delete id="deleteById">
        DELETE FROM course WHERE id = #{id}
    </delete>

    <select id="findById" resultType="com.kwang.study.course.pojo.Course">
        SELECT c.*, u.username as teacherName
        FROM course c LEFT JOIN users u ON c.teacher_id = u.id
        WHERE c.id = #{id}
    </select>

    <select id="findAllByClassId" resultType="com.kwang.study.course.pojo.Course">
        SELECT c.*, u.username as teacherName
        FROM course c LEFT JOIN users u ON c.teacher_id = u.id
        WHERE c.class_id = #{classId} ORDER BY c.create_time DESC
    </select>
</mapper>